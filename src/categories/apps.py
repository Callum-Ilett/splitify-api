"""App config for the categories app."""

from django.apps import AppConfig


class CategoriesConfig(AppConfig):
    """App config for the categories app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "categories"
