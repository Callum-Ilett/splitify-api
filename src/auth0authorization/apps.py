"""App configuration for the auth0authorization app."""

from django.apps import AppConfig


class Auth0AuthorizationConfig(AppConfig):
    """App configuration for the auth0authorization app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "auth0authorization"
